# inbox-analyzer
Inbox Analyzer is a  web application that connects to a user's mail(Gmail for now) account via OAuth 2.0 and provides a visual dashboard to understand inbox composition — which categories dominate, who the top senders are, and when emails arrive.
